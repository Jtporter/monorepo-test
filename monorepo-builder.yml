parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            phpunit/phpunit: '^7.3'
            symplify/monorepo-builder: '^6.1'

    # for "split" command
    directories_to_repositories:
        packages/vegetable: 'git@github.com:Jtporter/monorepo-test/Vegetable.git'
        packages/spinach: 'git@github.com:Jtporter/monorepo-test/Spinach.git'
